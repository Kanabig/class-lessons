account = 5000000
interestRatio = 0.05

account += (account * interestRatio)
account += (account * interestRatio)
account += (account * interestRatio)
account += (account * interestRatio)
account += (account * interestRatio)

print(f'{int(account):,}')
