# depth = int(input('수심을 입력하세요: '))
# surfaceTemp = 20
# tempFall = 0.7
# crt_temp = surfaceTemp - ((depth // 10) * tempFall)

# print(f'온도: {crt_temp}')

# inputs = []
# inputs.append(input('속도(km/h): '))
# inputs.append(input('시간(h): '))

# speed, hour = map(int, inputs)
# print(f'거리(km): {speed * hour}')

# workingTime = int(input('근무시간: '))
# addComputer = (24 // workingTime) + 1 if (24 % workingTime > 0) else 0
# print(f'필요한 컴퓨터 대수: {addComputer}')

# maskCount = int(input('구매할 마스크 개수(x340):'))
# payment = int(input('지불할 금액: '))

# change = payment - maskCount * 340
# print(change)

# print(f'{25 + (60 * 30) + (60 * 60 * 13)}')

# inputs = []
# inputs.append(input('국어: '))
# inputs.append(input('영어: '))
# inputs.append(input('수학: '))

# kor, eng, math = map(int, inputs)
# sum = kor + eng + math
# print(f'총점: {sum}, 평균: {sum / 3:.1f}')

# inputs = []
# inputs.append(input('밤 최저 기온: '))
# inputs.append(input('낮 최고 기온: '))

# lowTemp, highTemp = map(float, inputs)
# print(f'일교차: {highTemp - lowTemp}')

# length = int(input('길이(cm): '))
# print(f'{length * 0.39:.1f}inch')

